--!nonstrict
-- Lokasi: StarterPlayerScripts > SoundManagerInitializer_Client

-- Baris ini akan memuat SoundManager di client, yang akan
-- secara otomatis menjalankan logika client-side di dalamnya.
local SoundManager = require(game:GetService("ReplicatedStorage").Packages.SoundManager)
